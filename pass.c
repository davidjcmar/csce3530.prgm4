#include "header.h"

int format_tcp_h (struct tcp_head* tcp_h)
{
    printf ("In format tcp h\n"); // testing
    return 0;
}

int send_loop (int sock, char* buffer, int* len)
{
    printf ("In send loop\n");
    return 0;
}

int recv_loop ()
{
    printf ("In recv loop\n");
    return 0;
}